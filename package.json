{
  "name": "template-typescript",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev src",
    "build": "tsc --build --clean && tsc --build --verbose",
    "start": "node dist",
    "test": "jest src",
    "lint": "eslint . --fix --ext .js,.jsx,.ts,.tsx --ignore-path .gitignore && prettier . --write --ignore-path .gitignore"
  },
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "@types/node": "^16.3.0",
    "esbuild": "^0.12.15",
    "esbuild-jest": "^0.5.0",
    "eslint": "^7.30.0",
    "eslint-config-atp": "^0.8.0",
    "jest": "^27.0.6",
    "prettier": "^2.3.2",
    "ts-node-dev": "^1.1.8",
    "tslib": "^2.3.0",
    "typescript": "^4.3.5"
  }
}
